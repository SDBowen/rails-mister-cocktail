<%= form_for(@dose, url: cocktail_doses_path) do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.collection_select :ingredient_id, Ingredient.all, :id, :name  %>

  <%= f.submit %>
<% end %>
